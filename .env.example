# Web build: leave empty when SPA is served from the same origin as the API (Docker / App Service).
# VITE_API_BASE=

# easyapply-server HTTP port (local dev; Azure uses WEBSITES_PORT + PORT in App Service settings).
# PORT=8787
